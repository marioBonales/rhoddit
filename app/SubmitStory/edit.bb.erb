<div id="pageTitle">
		<h1>Edit <%= @submit_story.story%></h1>
</div>

<div id="toolbar">
    <%= link_to "Back", :action => :show, :id => @submit_story.object %>
    <%= link_to "Delete", :action => :delete, :id => @submit_story.object %>
</div>


<div id="content">
    <form method="POST" action="<%= url_for :action => :update %>">
    	<input type="hidden" name="id" value="<%= @submit_story.object %>"/>
            <table>
        		
                    <tr>
            			<td class="itemLabel">Story: </td>
              			<td class="itemValue"><input type="text" name="submit_story[story]" value="<%= @submit_story.story %>"/></td>
          		    </tr>
        		
            </table>
            <input type="submit" class="standardButton" value="Update" />
    </form>
</div>
